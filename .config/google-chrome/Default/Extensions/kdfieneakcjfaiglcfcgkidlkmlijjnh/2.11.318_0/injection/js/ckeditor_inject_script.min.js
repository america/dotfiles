/*
Code architecture and implementation by kristof.mihaly@nomo.hu
For Ginger Software Ltd.
Ginger Grammar Checker Chrome Extension
*/
(function() {
    (function() {
        function findCKEditorWindow(win) {
            if (win.CKEDITOR) {
                return win;
            } else if (win !== win.parent) {
                return findCKEditorWindow(win.parent);
            } else {
                return null;
            }
        }
        var frameWindow = findCKEditorWindow(window);
        if (frameWindow) {
            frameWindow.CKEDITOR.instances.editor.config.fullPage = false;
            frameWindow.CKEDITOR.instances.editor.on("loaded", function(e) {
                if (e.editor._) {
                    var getdata = function() {
                        return frameWindow.CKEDITOR.instances.editor._.data.replace(/<(?:gdiv)?[\s\S]*?<\/(?:gdiv)>/gim, "");
                    };
                }
                frameWindow.CKEDITOR.instances.editor.getData = getdata;
            });
        }
    })();
})();
//# sourceMappingURL=ckeditor_inject_script.min.js.map